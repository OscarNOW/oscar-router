I'm going to change the behaviour of this library. To do this, I want to first have failing tests so that I can then update the code to have the tests pass. This library has two test files: index.test.ts for runtime tests and types.test.ts for type tests.
You should change the tests (and add new ones if needed) for the behaviour described below. The tests may fail, because the code is not yet updated. Only change the tests, do not change the source code.

request should have a files property which is an object with string that maps to an object:
name: string;
mimeType: string;
buffer: Buffer;

you should be able to add a files schema in the validations. for the files property. transform should work.
the library also should also export lrFileSchema which is a zod schema for the file type.
idea.ts should be a correct example of this

formData fields should be parsed directly to the body property. an empty string should stay an empty string, not null.